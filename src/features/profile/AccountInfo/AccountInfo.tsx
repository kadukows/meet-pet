import React from 'react';

import { ProfilePageLayout } from '../helpers';

type Props = {};

const AccountInfo = (props: Props) => {
    return <ProfilePageLayout title="Account Info"></ProfilePageLayout>;
};

export default AccountInfo;
