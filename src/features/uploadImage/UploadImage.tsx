import React from 'react';

type Props = {};

const UploadImage = (props: Props) => {
    /*
    const [currentFile, setCurrentFile] = React.useState<any>(undefined);
    const [preview, setPreview] = React.useState<any>(undefined);
    const [progress, setProgress] = React.useState(0);
    const [imgInfos, setImgInfos] = React.useState<Array<any>>([]);

    const selectFile = React.useCallback(
        (event: React.FormEvent<HTMLInputElement>) => {
            const input = event.target as HTMLInputElement;
            setCurrentFile(input.files[0]);
        },
        []
    );
    */

    return <div>UploadImage</div>;
};

export default UploadImage;
